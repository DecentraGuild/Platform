# Tenant app (*.dguild.org)
# Netlify: set Base directory to "apps/tenant" for this site so this file is used.
# Publish path is relative to base (apps/tenant).

[build]
  base = "."
  command = "pnpm run build:tenant"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"
