# Discord bot (copy to .env, never commit .env)

# Required
DISCORD_BOT_TOKEN=             # Bot token from Discord Developer Portal
DISCORD_BOT_API_SECRET=         # Same value as API's DISCORD_BOT_API_SECRET (server-to-server auth)

# Required for /verify and API calls (default: http://localhost:3001)
API_BASE_URL=http://localhost:3001

# Optional: Application ID from Discord Developer Portal (bot can also fetch it at runtime)
DISCORD_APPLICATION_ID=

# Optional: Where users are sent when they use /verify. Placeholders: {{slug}}, {{token}}.
# Default if unset: https://{{slug}}.dguild.org/verify?token={{token}}
# Override for staging or white-label (e.g. different tenant domain).
# VERIFY_URL_TEMPLATE=https://{{slug}}.dguild.org/verify?token={{token}}
