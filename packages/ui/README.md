# UI package

Shared Vue components and design system for platform and tenant apps.

- Buttons, inputs, cards, layout primitives.
- Use Iconify (e.g. @iconify/vue) for icons; no emoji in UI.
- Themed per tenant when inside the tenant app (branding from core/tenant context).
