{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack pnpm install && corepack pnpm exec turbo run build --filter=api"
  },
  "deploy": {
    "startCommand": "sh -lc \"if [ \\\"$SERVICE_ROLE\\\" = worker ]; then corepack pnpm --filter api worker; else corepack pnpm --filter api start; fi\""
  }
}
