{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install && pnpm exec turbo run build --filter=api"
  },
  "deploy": {
    "startCommand": "sh -lc \"case \\\"$SERVICE_ROLE\\\" in worker) pnpm --filter api worker ;; *) pnpm --filter api start ;; esac\""
  }
}
